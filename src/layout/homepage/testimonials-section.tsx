interface Props {
  testimonials: Testimonial[];
}

const TestimonialSection: React.FC<Props> = ({
  testimonials,
}: Props): JSX.Element => {
  return <div></div>;
};

export { TestimonialSection };
